apiVersion: 1

providers:
  - name: sepapp
    orgId: 1
    folder: "SEPahan"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
