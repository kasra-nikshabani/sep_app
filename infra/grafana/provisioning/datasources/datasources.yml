apiVersion: 1

datasources:
  - name: Prometheus
    uid: sepapp-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false

  - name: Loki
    uid: sepapp-loki
    type: loki
    access: proxy
    url: http://loki:3100
    editable: false
